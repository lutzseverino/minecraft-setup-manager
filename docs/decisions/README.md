# Decisions

Decision records preserve choices with meaningful tradeoffs. Name records with a
four-digit sequence and kebab-case title, such as
`0001-keep-effects-in-rust.md`.

Each record contains `Status`, `Context`, `Decision`, `Consequences`, and
`Alternatives Considered`. Draft proposals and implementation checklists do not
belong here.

No decision records are published yet.
